/* global accounting */

export default function (number, decimalPlaces) {
  // var asNumber = window.parseFloat(number);
  // return +asNumber.toFixed(decimalPlaces);
  return accounting.toFixed(number, decimalPlaces);
}
