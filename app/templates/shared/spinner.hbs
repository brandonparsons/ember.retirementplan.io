{{#if spinkitSupported}}
  <div class="spinner">
    <div class="bounce1"></div>
    <div class="bounce2"></div>
    <div class="bounce3"></div>
  </div>
{{else}}
  <p class='lead text-center'>
    {{fontawesome-icon type="spinner" size='3x' spin=true withForcedSpace=false}}
  </p>
{{/if}}
